name: Build package

on:
  push:
    branches-ignore:
      - stable

jobs:
  build-package:
    name: Build package
    uses: MiguelRipoll23/workflows-global/.github/workflows/build-package.yml@main

    with:
      node-version: 16
